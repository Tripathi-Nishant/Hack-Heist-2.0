ENVIRONMENT=production
PORT=8000

# ── LLM (optional — rule-based fallback if not set)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Database (AWS RDS PostgreSQL) ─────────────────
# Get endpoint from RDS console after Step 2
DB_HOST=driftwatch-prod.c54oomkia9m3.ap-south-1.rds.amazonaws.com
DB_NAME=driftwatch
DB_USER=driftwatch_admin
DB_PASSWORD=DriftWatch
DB_PORT=5432

# ── AWS ───────────────────────────────────────────
AWS_REGION=ap-south-1
EC2_PUBLIC_IP=65.2.180.52

# S3 bucket for CSV uploads and fingerprints
S3_DATA_BUCKET=driftwatch-data-067773914232-ap-south-1-an 
# SNS topic ARN for email alerts
# Get from SNS console after Step 4
SNS_TOPIC_ARN=arn:aws:sns:ap-south-1:067773914232:driftwatch-alerts

# SES verified email addresses
SES_FROM_EMAIL=tripathinishant498@gmail.com
ALERT_EMAIL=tripathinishant498@gmail.com

# ── Drift thresholds (optional — defaults shown) ──
PSI_WARNING_THRESHOLD=0.10
PSI_CRITICAL_THRESHOLD=0.20